import 'package:flutter/material.dart';
import 'package:ulastir/app/theme/designer.dart';

class Slack extends StatelessWidget {
  const Slack({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: Designer.slackHeight,
    );
  }
}
